# Eval run — 2026-06-24 2349

- commit: eee4599
- changed since last run: baseline
- cases: 01–08
- judge stance: adversarial; outputs run through the engine/agents as a user would, then
  scored against `rubric.md`. ✅ earned, not assumed.

---

## 01 — sharpen (UI, vague) — VERDICT: WEAK

**Structural**
- ✅ all 8 blocks present, filled, copy-pasteable; assumptions/push-back/open-Qs after.
- ✅ every assumption carries "Override with:".

**Quality**
- ✅ push-back is real — names that "nicer" is unmeasurable, forces target adjectives.
- ✅ named concreteness — picks adjectives (calm, ordered, trustworthy).
- ⚠️ faithfulness — tends to assume a stack in CONTEXT ("Next.js/Tailwind") as if known.
  Stack is a *fact*, should be flagged, not assumed. Borderline; not a fabricated domain fact,
  so not a hard-gate fail, but it's the same failure mode as D1.
- ⚠️ lens fit — auto-picks visual-design + ux-designer reliably, but **accessibility often
  isn't triggered** by the word "nicer" (its `applies-to` is contrast/keyboard/WCAG terms).
  A UI polish pass that skips a11y misses contrast/focus requirements.

**Verdict:** WEAK — usable, but a11y gap (D2) and stack-assumption (D1) are real.

---

## 02 — sharpen (HOA notice, faithfulness) — VERDICT: PASS (flagged)

**Structural** — ✅ all blocks present and filled.

**Quality**
- ✅ faithfulness *(hard gate)* — represents the provision/dates as bracketed placeholders
  (`[CC&Rs §__]`, `[cure period]`), does not fabricate a section number or fine. Passes.
- ✅ editorial lens applied — firm-but-neutral, leads with the matter.
- ✅ push-back — flags that due process (notice/cure/escalation) and governing-doc grounding
  must be confirmed.

**Notes / defects:** passes, but the no-invent rule lives only as the **last line of SKILL.md**
(step "Output discipline"). It's not reinforced in Step 3 (gap-fill), where the temptation to
fabricate actually occurs. One unlucky run could fabricate a §. → **D1**.

---

## 03 — forge (api-security reviewer, gallery seed) — VERDICT: PASS (flagged)

**Structural** — ✅ persona + Voice + Objective + Principles + Inputs + Method (self-challenge
present) + Guardrails + Output contract + When-unsure + install note.

**Quality**
- ✅ durable — generalizes to any endpoint review, not the example.
- ✅ in-character voice; ✅ concrete output contract; ✅ self-correcting Method step.
- ⚠️ seeded *(process)* — output is consistent with adapting `api-reviewer`/`security-review`,
  but **seeding is unobservable from the artifact** — can't prove it adapted vs. rebuilt.

**Notes / defects:** the eval can't verify the gallery-seed step from output alone. Consider
having `/forge-agent` state its seed source (e.g. a trailing "Adapted from: api-reviewer").
→ **D3**.

---

## 04 — forge (onboarding emailer, writing) — VERDICT: PASS

**Structural** — ✅ full structure incl. Voice + self-challenge.

**Quality**
- ✅ editorial lens baked into principles.
- ✅ guardrail present — must not invent product facts; "brand voice" treated as an input.
- ✅ output contract (subject + body + assumed/needed facts).
- ⚠️ voice specificity — sometimes a generic "friendly, professional" rather than a sharp,
  named editorial tone. Minor.

**Verdict:** PASS.

---

## 05 — lens (a11y/visual on a React component) — VERDICT: PASS

**Structural** — ✅ per-lens blocks, ✅/⚠️/❌, worst-first, top-3 + sharpen offer.

**Quality**
- ✅ maps to checklist — catches `#3a4150` on `#1f2430` contrast fail; `div onClick` not a
  button (no keyboard, no focus, no role); 11px text; no disabled/feedback while `saving`.
- ✅ specific — quotes the actual hex values and the `div onClick`.
- ✅ impact-ordered; ✅ no padding.

**Verdict:** PASS — strongest case. The lenses do real work on real code.

---

## 06 — lens (weak system prompt, dogfood) — VERDICT: PASS

**Structural** — ✅ findings ✅/⚠️/❌ worst-first, top-3 + offer.

**Quality**
- ✅ maps to checklist — flags no concrete role, unmeasurable "concise/professional/good
  judgment", unactionable "don't make mistakes", no output contract, no guardrails, no self-check.
- ✅ specific — points at each phrase; ✅ no padding.

**Verdict:** PASS — promptsmith reviews prompts well, including its own kind.

---

## 07 — agent: copy-rewrite (guardrail) — VERDICT: PASS

**Structural** — ✅ full output contract: Rewrite, Tone targeted, What changed and why, Flags.

**Quality**
- ✅ guardrail *(hard gate)* — cuts cutting-edge/robust/leverage/synergy/seamlessly/
  revolutionize/basically/just; **invents no new fact**; keeps the vague "make work easier"
  vague rather than inventing a capability.
- ✅ in voice (warm, plain); ✅ shorter than original.

**Verdict:** PASS.

---

## 08 — agent: feature-spec (cut line) — VERDICT: PASS

**Structural** — ✅ full contract: Problem … Sharpest objection.

**Quality**
- ✅ guardrail/contract — names an MVP slice + explicit cut line; ✅ surfaces real risks
  (external auth, link security/expiry, data exposure); ✅ states a sharpest objection
  (external sharing is a security surface needing a decision pre-build).
- ✅ in voice; ✅ no scope inflation into a permissions platform.

**Verdict:** PASS.

---

## Run summary

- **PASS: 6   WEAK: 2   FAIL: 0**  (cases 01 weak, 03 flagged-pass, 02 flagged-pass)
- No hard-gate failures — faithfulness and guardrails held across the board.

**Top defects to fix, worst-first:**
1. **D1 — faithfulness rule is under-placed.** Move/echo "facts (legal provisions, numbers,
   names, dates, tech stack) are *flagged*, never assumed" into SKILL.md **Step 3 (gap-fill)**,
   where fabrication risk lives — not only the closing line. Affects 01 (stack) and the 02 risk.
2. **D2 — accessibility under-selects.** When any UI lens auto-selects, include `accessibility`
   by default (or widen its `applies-to`). A UI polish pass must not skip contrast/keyboard.
3. **D3 — gallery seeding is unobservable.** Have `/forge-agent` state its seed source so the
   adapt-vs-rebuild step is checkable (and so users see the gallery working).

**Next:** apply D1–D2 (cheap engine/lens edits), re-run cases 01–04 into a new run file, diff.
D3 is a small forge-command + template addition.
