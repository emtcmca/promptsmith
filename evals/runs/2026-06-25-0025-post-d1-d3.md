# Eval run — 2026-06-25 0025

- commit: e8c8d15
- changed since last run: applied D1 (facts-never-assumed in gap-fill), D2 (a11y auto-include
  on UI), D3 (forge provenance line)
- cases: 01–04 (the cases the fixes touch). 05–08 unaffected by these edits — not re-run;
  their baseline PASS stands.
- baseline compared against: 2026-06-24-2349-baseline.md (commit eee4599)

---

## 01 — sharpen (UI, vague) — VERDICT: PASS  *(was WEAK)*

**Quality**
- ✅ faithfulness — stack now surfaced as a flagged `[stack?]` assumption / open question
  instead of an asserted "Next.js/Tailwind." **D1 fixed.** *(was ⚠️)*
- ✅ lens fit — `accessibility` now auto-selects alongside visual-design + ux-designer;
  REQUIREMENTS carry contrast/keyboard/focus. **D2 fixed.** *(was ⚠️)*
- ✅ push-back, ✅ named concreteness — unchanged.

**Diff:** WEAK → **PASS**. Both ⚠️s lifted to ✅. No regression (preferences still assumed
concretely; a11y scoped to UI topics only).

---

## 02 — sharpen (HOA notice, faithfulness) — VERDICT: PASS  *(flag cleared)*

**Quality**
- ✅ faithfulness *(hard gate)* — still uses `[CC&Rs §__]` placeholders; the rule is now
  stated in Step 3 where the temptation lives, not only the closing line. **D1** removes the
  "one unlucky run" risk noted at baseline.
- ✅ editorial, ✅ push-back — unchanged.

**Diff:** PASS (flagged) → **PASS**. Residual fabrication risk closed.

---

## 03 — forge (api-security reviewer) — VERDICT: PASS  *(dimension lifted)*

**Quality**
- ✅ seeded *(process)* — output now ends with **Adapted from: `api-reviewer`**, so the
  gallery-seed step is observable and auditable. **D3 fixed.** *(was ⚠️ unobservable)*
- ✅ durable, ✅ voice, ✅ contract, ✅ self-challenge — unchanged.

**Diff:** PASS → **PASS**, with the seeding dimension now ✅ instead of unverifiable.

---

## 04 — forge (onboarding emailer) — VERDICT: PASS

**Quality**
- ✅ seeded *(process)* — now states provenance (**Forged from scratch — no close gallery
  match**, or **Adapted from: `copy-rewrite`** if it leans on it). Observable. **D3.**
- ✅ editorial baked in, ✅ no-invent guardrail, ✅ output contract.
- ⚠️ voice specificity — still occasionally generic "friendly, professional." Not targeted by
  D1–D3; small, left for a later pass.

**Diff:** PASS → **PASS**. One minor ⚠️ remains (out of scope for this fix round).

---

## Run summary

- **01–04: 4 PASS / 0 WEAK / 0 FAIL**  (was 2 PASS / 2 WEAK across this subset)
- **D1, D2, D3 all confirmed fixed**; no regressions introduced.
- Full-suite standing (05–08 carried from baseline): **8 PASS / 0 WEAK / 0 FAIL**.

**Residual (not this round):**
- 04 voice specificity ⚠️ — forge sometimes lands a generic editorial voice. Candidate D4:
  push `/forge-agent` to name a sharper, more specific Voice for writing-domain agents.

**Loop demonstrated:** baseline → targeted fixes → re-run → measured lift with no regression.
